import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  closeButton: {
    position: 'absolute',
    justifyContent: 'center',
    alignItems: 'center',
    left: 5,
    top: 5,
  },
});
