import {StyleSheet} from 'react-native';

const FilterCardsStyles = StyleSheet.create({
  card: {
    padding: 5,
    borderRadius: 5,
    backgroundColor: '#FFFFFF',
  },
});

const FilterListStyles = StyleSheet.create({
  scroll: {},
  card: {
    marginHorizontal: 5,
  },
});

export {FilterCardsStyles, FilterListStyles};
